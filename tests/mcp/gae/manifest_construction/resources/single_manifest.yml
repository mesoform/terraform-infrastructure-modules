components:
  common:
    env: flex
  specs:
    app1:
      name: app1
    default:
      name: app2
      env: standard
      root_dir: app2 # optional. Defaults to AS name
      build_dir: build  #  where build output files are located. It is also where mmcf-manifest.json needs to be

